# this is docker subnet, no intersection with local host
SERVICES_SUBNET=<xx.xx.xx.xx> # e.g. 192.168.11.0/20
SERVICES_SUBNET_GATEWAY=<xx.xx.xx.xx> # e.g. 192.168.11.1

NGINX_REVERSE_PROXY_IP=<static IP from svc-subnet> # e.g. 192.168.11.10
DNSMASQ_IP=<static IP from svc-subnet> # e.g. 192.168.11.11
SQUID_IP=<static IP from svc-subnet> # e.g. 192.168.11.12
SQUID_PORT=<xxxx> # 3128 by default
CADVISOR_IP=<static IP from svc-subnet> # e.g. 192.168.11.13
